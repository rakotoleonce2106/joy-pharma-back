<div {{ attributes }}>
    {{ form_start(form, { attr: {
        class: 'space-y-4',
        'id': 'store-category-form'
    } }) }}
    {{ form_row(form.label) }}
    {{ form_row(form.description) }}

    <div class="items-center justify-center gap-2 mt-6 hidden turbo-frame:flex">
        <twig:ui:button:root href="{{ path('admin_store_category') }}" variant="outline">
            {{ 'Discard'|trans }}
        </twig:ui:button:root>
        <twig:ui:button:root formnovalidate>{{ 'Update Store Category'|trans }}</twig:ui:button:root>
    </div>

    {{ form_end(form) }}
</div>
