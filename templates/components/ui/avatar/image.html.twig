<img {{ attributes.add(stimulus_target('ui--avatar', 'image')).without('class') }}
        loading="lazy"
        class="{{ ['aspect-square h-full w-full', attributes.render('class')] | tailwind_merge }}"
        hidden>