<div {{ attributes.add(stimulus_target('ui--sheet', 'trigger')).without('class') }}
        data-state="closed"
        aria-haspopup="dialog"
        aria-expanded="false"
        class="{{ ['', attributes.render('class')] | tailwind_merge }}">
    {% block content %}{% endblock %}
</div>