╔══════════════════════════════════════════════════════════════════════╗
║                                                                      ║
║         ✅  FIX: DOCKER IMAGE NAMING ET TAGS  ✅                    ║
║                                                                      ║
╚══════════════════════════════════════════════════════════════════════╝

🔧 PROBLÈME IDENTIFIÉ
═══════════════════════════════════════════════════════════════════════

Erreur:
─────────────────────────────────────────────────────────────────────
❌ "failed to resolve reference docker.io/user/app-php:latest: not found"
❌ Image recherchée: user/app-php:latest
❌ Image réelle: user/joy-pharma-back:preprod

Causes:
─────────────────────────────────────────────────────────────────────
1. Incohérence du nom d'image:
   - compose.yaml utilisait: ${IMAGES_PREFIX:-}app-php
   - Build workflow créait: joy-pharma-back
   
2. Tag non spécifié:
   - compose.yaml: pas de tag → :latest par défaut
   - Workflow: tag "preprod" non passé au compose

3. Variables manquantes:
   - IMAGE_TAG non défini dans .env

═══════════════════════════════════════════════════════════════════════

✅ SOLUTION IMPLÉMENTÉE
═══════════════════════════════════════════════════════════════════════

1. Standardisation du Nom d'Image:
─────────────────────────────────────────────────────────────────────
AVANT: image: ${IMAGES_PREFIX:-}app-php
APRÈS:  image: ${IMAGES_PREFIX:-}joy-pharma-back:${IMAGE_TAG:-latest}

2. Ajout de IMAGE_TAG dans .env:
─────────────────────────────────────────────────────────────────────
echo "IMAGE_TAG=${IMAGE_TAG}" >> .env

3. Export des Variables:
─────────────────────────────────────────────────────────────────────
export IMAGES_PREFIX="${{ secrets.DOCKERHUB_USERNAME }}/"
export IMAGE_TAG="preprod" # ou autre tag

═══════════════════════════════════════════════════════════════════════

📦 FICHIERS MODIFIÉS
═══════════════════════════════════════════════════════════════════════

✅ compose.yaml
   → image: ${IMAGES_PREFIX:-}joy-pharma-back:${IMAGE_TAG:-latest}
   → Nom cohérent avec le workflow de build
   → Tag paramétrable

✅ compose.override.yaml (dev)
   → image: ${IMAGES_PREFIX:-}joy-pharma-back:${IMAGE_TAG:-dev}
   → Tag "dev" par défaut en développement

✅ .github/workflows/deploy-server.yml
   → Ajout de IMAGE_TAG dans .env
   → Export des variables IMAGES_PREFIX et IMAGE_TAG
   → Logs améliorés pour debug

═══════════════════════════════════════════════════════════════════════

🔍 STRUCTURE DES IMAGES
═══════════════════════════════════════════════════════════════════════

Format Complet:
─────────────────────────────────────────────────────────────────────
REGISTRY/USERNAME/REPOSITORY:TAG

Exemples:
─────────────────────────────────────────────────────────────────────
Production:   docker.io/youruser/joy-pharma-back:preprod
Development:  docker.io/youruser/joy-pharma-back:dev
Main branch:  docker.io/youruser/joy-pharma-back:main
Latest:       docker.io/youruser/joy-pharma-back:latest

Variables Docker Compose:
─────────────────────────────────────────────────────────────────────
IMAGES_PREFIX = youruser/
IMAGE_TAG = preprod
→ Résultat: youruser/joy-pharma-back:preprod

═══════════════════════════════════════════════════════════════════════

🔧 VARIABLES D'ENVIRONNEMENT
═══════════════════════════════════════════════════════════════════════

Dans .env (Serveur):
─────────────────────────────────────────────────────────────────────
IMAGES_PREFIX=youruser/
IMAGE_TAG=preprod

Génération Automatique (.github/workflows/deploy-server.yml):
─────────────────────────────────────────────────────────────────────
IMAGE_TAG="${{ inputs.image-tag }}"  # ou "preprod" par défaut
IMAGES_PREFIX="${{ secrets.DOCKERHUB_USERNAME }}/"
echo "IMAGE_TAG=${IMAGE_TAG}" >> .env
echo "IMAGES_PREFIX=${IMAGES_PREFIX}" >> .env

═══════════════════════════════════════════════════════════════════════

✅ VÉRIFICATIONS
═══════════════════════════════════════════════════════════════════════

1. Vérifier les Variables:
─────────────────────────────────────────────────────────────────────
cat .env | grep -E "IMAGES_PREFIX|IMAGE_TAG"

# Sortie attendue:
IMAGES_PREFIX=youruser/
IMAGE_TAG=preprod

2. Vérifier l'Image Complète:
─────────────────────────────────────────────────────────────────────
docker compose config | grep image:

# Sortie attendue:
    image: youruser/joy-pharma-back:preprod

3. Vérifier l'Existence de l'Image:
─────────────────────────────────────────────────────────────────────
docker pull youruser/joy-pharma-back:preprod

# Ou vérifier sur Docker Hub:
# https://hub.docker.com/r/youruser/joy-pharma-back/tags

═══════════════════════════════════════════════════════════════════════

🚀 WORKFLOW DE BUILD ET DÉPLOIEMENT
═══════════════════════════════════════════════════════════════════════

1. Build (deploy-build.yml):
─────────────────────────────────────────────────────────────────────
Push vers "preprod" 
  ↓
Build de l'image Dockerfile (target: frankenphp_prod)
  ↓
Tag: youruser/joy-pharma-back:preprod
  ↓
Push vers Docker Hub
  ↓
Image disponible: docker.io/youruser/joy-pharma-back:preprod

2. Deploy (deploy-server.yml):
─────────────────────────────────────────────────────────────────────
Copie des fichiers de config
  ↓
Génération .env avec Infisical
  ↓
Ajout variables Docker:
  - IMAGES_PREFIX=youruser/
  - IMAGE_TAG=preprod
  ↓
Pull de l'image:
  docker compose pull
  → Pull: youruser/joy-pharma-back:preprod ✅
  ↓
Démarrage:
  docker compose up -d

═══════════════════════════════════════════════════════════════════════

🛠️ DÉVELOPPEMENT LOCAL
═══════════════════════════════════════════════════════════════════════

Build Local:
─────────────────────────────────────────────────────────────────────
# Sans variables (utilise les défauts)
docker compose build

# Image créée: joy-pharma-back:dev (tag par défaut)

Avec Variables:
─────────────────────────────────────────────────────────────────────
export IMAGES_PREFIX="myuser/"
export IMAGE_TAG="local"
docker compose build

# Image créée: myuser/joy-pharma-back:local

Démarrage:
─────────────────────────────────────────────────────────────────────
docker compose up -d

# Utilise l'image locale ou la build si nécessaire

═══════════════════════════════════════════════════════════════════════

📋 TAGS RECOMMANDÉS
═══════════════════════════════════════════════════════════════════════

Environnements:
─────────────────────────────────────────────────────────────────────
dev         → Développement local
staging     → Pré-production/Staging
preprod     → Pré-production
prod        → Production
latest      → Dernière version stable (main/master)

Par Branche:
─────────────────────────────────────────────────────────────────────
main        → Branche principale
feature-x   → Branche feature
hotfix-123  → Correctif urgent

Par Version:
─────────────────────────────────────────────────────────────────────
v1.0.0      → Version sémantique
v1.0        → Version majeure.mineure
v1          → Version majeure

Par Commit:
─────────────────────────────────────────────────────────────────────
sha-abc1234 → Hash du commit
main-abc123 → Branche + hash court

═══════════════════════════════════════════════════════════════════════

🐛 TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════

Erreur: "Image not found"
─────────────────────────────────────────────────────────────────────
Causes possibles:
1. Image pas encore buildée et pushée
2. Tag incorrect
3. Username Docker Hub incorrect
4. Image privée sans authentification

Solutions:
# Vérifier le nom complet de l'image
docker compose config | grep image:

# Vérifier sur Docker Hub
https://hub.docker.com/r/youruser/joy-pharma-back/tags

# Build et push manuellement
docker compose -f compose.yaml -f compose.prod.yaml build
docker push youruser/joy-pharma-back:preprod

# Vérifier l'authentification
docker login
docker pull youruser/joy-pharma-back:preprod

Erreur: "WARNING: Some service image(s) must be built"
─────────────────────────────────────────────────────────────────────
Cause: compose.override.yaml définit un "build" en dev

Solution: En production, utiliser sans override:
docker compose -f compose.yaml -f compose.prod.yaml pull

Variables Non Définies
─────────────────────────────────────────────────────────────────────
# Vérifier les variables
echo $IMAGES_PREFIX
echo $IMAGE_TAG

# Les définir si manquantes
export IMAGES_PREFIX="youruser/"
export IMAGE_TAG="preprod"

# Ou les ajouter à .env
echo "IMAGES_PREFIX=youruser/" >> .env
echo "IMAGE_TAG=preprod" >> .env

═══════════════════════════════════════════════════════════════════════

📚 RÉSUMÉ DES CHANGEMENTS
═══════════════════════════════════════════════════════════════════════

AVANT:
❌ image: ${IMAGES_PREFIX:-}app-php (nom incorrect)
❌ Pas de tag spécifié
❌ IMAGE_TAG non défini dans .env

APRÈS:
✅ image: ${IMAGES_PREFIX:-}joy-pharma-back:${IMAGE_TAG:-latest}
✅ Nom cohérent avec le build workflow
✅ Tag paramétrable et défini dans .env
✅ Variables exportées dans le workflow

RÉSULTAT:
✅ Pull de l'image fonctionne
✅ Déploiement réussit
✅ Images cohérentes dev/prod

═══════════════════════════════════════════════════════════════════════

            ✅  Nommage d'image Docker corrigé !  ✅

═══════════════════════════════════════════════════════════════════════
