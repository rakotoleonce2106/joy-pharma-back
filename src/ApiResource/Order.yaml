App\Entity\Order:
    operations:
        post_order:
            class: ApiPlatform\Metadata\Post
            uriTemplate: "/order"
            controller: App\Controller\Api\Product\CreateOrderPaymentIntent
            validationContext:
                groups: ['order:create' ]
            normalizationContext:
                groups: [ 'id:read',"order:read" ]
            openapi:
                summary: 'Create a order'
                description: 'Create a order'

                
        get_order:
            method: 'GET'
            class: ApiPlatform\Metadata\Get
            uriTemplate: '/order/{id}'
            normalizationContext:
                groups: ['order:read']
            openapi:
                summary: 'Get a order'
                description: 'Get a order by id'
                parameters:
                    - name: 'id'
                      in: 'path'
                      description:  'The id of the order'

        get_categories:
            method: 'GET'
            class: ApiPlatform\Metadata\GetCollection
            uriTemplate: '/orders'
            normalizationContext:
                groups: ['id:read','order:read']
            openapi:
                summary: 'Get all ordes'
                description: 'Get all orders'
                

    normalizationContext:
        groups: ['id:read','order:read']
                      
